import { useEffect } from "react";

/**
 * Placeholder hook so the app builds.
 * If you later add real theme logic, replace this safely.
 */
export function useThemeShift() {
  useEffect(() => {
    // no-op
  }, []);
}
